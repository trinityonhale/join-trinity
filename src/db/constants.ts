export enum Role {
    admin = 'admin',
    user = 'user'
}

export enum QuestUrgency {
    low = 1,
    medium = 2,
    high = 3
}

export enum QuestStatus {
    open = 'open',
    closed = 'closed'
}